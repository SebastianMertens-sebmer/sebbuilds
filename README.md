# Seb Builds

Personal builder website for Sebastian, hosted at `sebmer.com`.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run typecheck
npm run build
```

Content lives in `content/projects` and `content/notes` as MDX files with frontmatter.
